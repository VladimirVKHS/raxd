# Guardian Report: security-guardian — bootstrap-cli

## Summary
Артефакты `threat-model.md` и `security-requirements.md` соразмерны каркасу и сделаны добросовестно: каждый применимый к скелету пункт baseline (права `0700`/`0600` §1/§2, отсутствие секретов §4, Docker-only §6, отсутствие exec и «честная» заглушка `serve` §3) отражён проверяемыми требованиями с привязкой к AC spec и контрактам plan; ни один неприменимый пункт baseline (§1 ключи, §2 TLS, §3 exec, §4 аудит, §5 дистрибуция) не пропущен молча — для каждого есть запись в threat-model с обоснованием, компенсирующим контролем и привязкой к будущему task-id. Тихих ослаблений baseline не обнаружено. Замечания — минорные уточнения формулировок, не блокируют. Verdict: pass.

## Checklist
- [x] Покрыты все разделы SECURITY-BASELINE (1-5): применимые активными требованиями, неприменимые — записями-границами с обоснованием и task-id.
- [x] У каждого риска есть смягчение (9 актуальных + 5 границ).
- [x] Нет «упрощений» безопасности ради скорости; baseline соблюдён как контракт.
- [x] Каждое требование проверяемо (Mode().Perm()==0700, grep на секреты, тест ненулевого exit serve, тесты config.Load).
- [x] Это требования, а не код.
- [x] Неприменимые пункты baseline явно зафиксированы, а не тихо пропущены.
- [x] Артефакты на русском языке.
- [x] Соразмерность: нет выдуманных требований к несуществующей сети; обязательный минимум не ослаблен.
- [x] Заглушка serve не открывает порт; нет exec в каркасе; status/баннер/version не раскрывают секреты.
- [x] Эскалации = None с обоснованием.

## Issues (минорные, не блокирующие)
- [ ] Issue 1: критерий прав будущих файлов `0600` через grep «режим > 0600» неоднозначен для qa.
  - Где: `security-requirements.md`, раздел «Права и расположение…».
  - Что делать: переформулировать как «в `internal/config` нет вызовов os.WriteFile/os.Create/OpenFile; при появлении — режим строго 0600» (однозначный grep по именам функций).
- [ ] Issue 2: требование «баннер на stderr» смешивает UX-канал с требованием безопасности §4.
  - Где: `security-requirements.md`, раздел «Отсутствие секретов…»; `threat-model.md` (актив «Поверхность вывода CLI»).
  - Что делать: оставить как требование безопасности только «вывод баннера не содержит секретов»; пометку про stderr вынести как «(канал — по контракту plan/cli-ux, не пункт baseline)».

## Looks good
- Образцовое разделение «активные требования» vs «будущие границы» с привязкой каждой границы к task-id; red line «безопасность не опциональна» соблюдена.
- Сильная проверяемость активных требований (конкретные тесты/grep).
- Корректно пойман риск наследования прав (каталоги 0700 заранее, чтобы будущие секреты не легли в world/group-readable каталог).
- serve-заглушка и отсутствие exec покрыты явными запрет-требованиями (net.Listen/exec.Command/sh -c).

## Verdict
pass

> Примечание дирижёра: verdict pass. Два минорных уточнения формулировок (Issue 1, 2) — не ослабление baseline, не требуют отдельного цикла; учесть при следующей правке security-requirements или передать как уточнение developer/qa.
