# pm-guardian — задача `distribution` (раунд 1)

**Verdict: needs-changes.** Guardian (read-only). Дата: 2026-05-22. Сохранено дирижёром (red line 1).

Проверен `specs/distribution/spec.md` (16 AC, Q1-Q7) против контракта PM и red lines.

## Подтверждено
- Все секции на месте, русский, кода нет (187 строк — обосновано плотностью, связность по AC16).
- 16/16 AC с разделом «Тест:»; AC по ошибкам/edge (AC3 подмена хэша, AC4 неподдерж. платформа, AC6, AC9).
- baseline §5/§6 покрыт точно: AC2 (set -euo pipefail/тело-в-функции/trap), AC3 (SHA256SUMS hard-fail),
  AC12/13 (чистый debian/ubuntu контейнер + честное ограничение macOS), AC14 (CI в контейнере), AC8 (4 цели
  +SHA256SUMS), AC10 (ldflags).
- **Нет architecture-leak:** goreleaser/GitHub Actions/конкретные пути в Open Questions (Q1/Q4/Q5), не в AC.
- Ограничение remote честно: AC12/Q3/Q5 (мок-источник артефактов в контейнере).
- Out of Scope: Windows/32-бит, сервис-механизм (service-install), пакетные менеджеры, нотаризация без
  сертификата, хостинг артефактов.

## Issue (needs-changes)
**AC9 частично непроверяем как stand-alone (строка ~78).** «целевой каталог установки лежит в PATH (конкретный
путь — Q4)» — заголовочный критерий не даёт однозначного pass/fail пока Q4 открыт (неясно, считать ли
~/.local/bin без export PATH провалом). Fix: добавить fallback-критерий, проверяемый независимо от конкретного
пути — «итоговый каталог присутствует в $PATH на момент выполнения скрипта» — ИЛИ пометить AC9 как
«разблокируется после закрытия Q4» (precondition в Open Questions).

## Итог
needs-changes (1 непроверяемый AC). После правки — инлайн-проверка дирижёра, повторный полный гейт не нужен.
