# cli-ux-guardian — service-purge

**Verdict: pass** (после двух раундов правок)

Артефакт: `specs/service-purge/ux-spec.md`.

Закрыты замечания:
- Issue 1: макет барьера приведён к плейсхолдерам `<state-dir>`/`<config-dir>` (значения из `DefaultConfigForGOOS`), без двойных платформенных строк.
- Issue 2: ASCII-макеты всех сценариев выровнены под `  %-14s  value`; метки идемпотентного сценария ≤14 (`not running`/`not installed`/`absent`).
- Issue 3: уточнено, что `dirs_removed`/`dirs_absent` — пути каталогов верхнего уровня (не содержимое), SR-124.
- Доп.: удалён блок Go-кода из раздела «Согласованность со стилем service.go» (red line cli-ux — без Go-кода); заменён текстовым описанием формата.

Одобрено: барьер SR-114/SR-115 (без `--yes` ничего не удаляется, предупреждение про необратимость и keys.db/аудит), без секретов SR-124, AC2 byte-for-byte (uninstall без --purge не меняется), идемпотентность AC3 визуально отличима (Faint), потоки stdout/stderr согласованы, автор в баннере, NO_COLOR/узкий терминал учтены. Тексты английские.
