# security-guardian — service-purge

**Verdict: pass**

Артефакты: `threat-model.md`, `security-requirements.md` (SR-114…SR-127; коллизий с максимальным занятым SR-113 нет).

Покрытие 12 рисков, каждый со смягчением: R-1 потеря keys.db/аудита (барьер `--yes` SR-114 + предупреждение SR-115), R-2 repudiation (аудит ДО удаления SR-116), R-3 чужой пользователь (verifyTargetUser SR-117), R-4 опасный путь (validatePurgePath SR-118), R-5 симлинк/TOCTOU (EvalSymlinks внутри раскладки SR-119), R-6 command-injection (exec без shell SR-120, наследует SR-91), R-7 эскалация (ErrPermission SR-121), R-8 грязное состояние (порядок шагов SR-122), R-9 идемпотентность ≠ маскировка прав (SR-123), R-10 утечка секретов (SR-124).

Baseline §3 (exec/привилегии) и §4 (аудит) покрыты; §1/§2 неприменимы (локальная фича без сетевого/ключевого вектора — keys.db лишь как удаляемый актив); §6 — SR-126/SR-127.

**SR-95/SR-96 закрыты корректно:** наследуются из `service-install/security-requirements.md` (SR-95=нейтральные сообщения→конкретизирован SR-124; SR-96=stdlib/Docker→подтверждён SR-127). Повисших ссылок нет (проверено по файлу service-install).

Остаточные риски эскалированы: ОР-1 (нет бэкапа keys.db — компенсируется двойным барьером, ответственность оператора → tech-writer), ОР-2 (остаточный TOCTOU под root — принят, «локальный root всевластен»). Ослабления baseline нет.
