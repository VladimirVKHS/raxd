# Guardian Report: cli-ux-guardian — key-management

## Summary
Артефакт проработан тщательно: безопасность вывода (SR-11..15) зафиксирована явно и последовательно, каналы stdout/stderr обоснованы, все ключевые команды имеют ASCII-макеты, тексты ошибок `error:`/`hint:`. Два дефекта: Go-блок в теле спеки нарушает red line cli-ux; регистр `Hint:`/`hint:` расходится с bootstrap в тексте пустого списка + неверное обоснование.

## Issues
- [ ] Issue 1 (red line): Go-блок в разделе «Цвета и стиль» → «Путь подключения» (≈строки 417-424) содержит `if noColor { return plainWarning }` — реализация, а не вывод/макет. Пометка «псевдо-фрагмент» не снимает нарушения «Go-код запрещён».
  - Что делать: заменить declarative-текстом без Go-синтаксиса (как сделано в bootstrap ux-spec): «При NO_COLOR или --no-color — строка WARNING без lipgloss-стилей; иначе — со стилем». Код-блок убрать.
- [ ] Issue 2 (консистентность): пустой список (≈строка 205) использует `hint:` строчными, а bootstrap ux-spec (≈строка 238) в том же контексте — `Hint:` с заглавной; строка 208 ложно утверждает «строчный hint: соответствует стилю bootstrap».
  - Что делать: унифицировать на строчный `hint:` ВЕЗДЕ (включая пустой список); убрать/исправить ложное обоснование. (Решение дирижёра: строчный hint: — единое правило; пустой список bootstrap был иллюстрацией для заглушки, реальный контракт списка — здесь; bootstrap не трогаем.)

## Looks good
- Безопасность вывода задокументирована образцово: отдельный раздел с 6 явными запретами (тело ключа только один раз на stdout при create; list/ошибки/аудит — без тела ключа). Ни один макет не нарушает.
- Раскладка каналов обоснована таблицей с примерами перенаправления (`> key.txt`, `2>/dev/null`, `| grep`); trade-off box-рамки на stdout явно признан.
- Тексты ошибок полные и действенные (ErrCorrupt, permission denied, flock-таймаут, ошибка записи, несуществующий id, уже revoked); кейс «ключ сгенерирован, но не записан» проработан с двумя hint.

## Verdict (раунд 1)
needs-changes

---

## Повторная проверка (раунд 2)
Оба дефекта устранены:
- Issue 1: Go-блок удалён, заменён declarative-описанием NO_COLOR без Go-синтаксиса (подтверждено grep).
- Issue 2: `hint:` унифицирован строчными везде, добавлено явное правило; заглавный `Hint:` остался только в пояснительном предложении (не макет).
Открытых issues нет.

## Verdict (финал)
pass

---

## Раунд 3 — выравнивание `key list` под полный 16-hex id (Q5), 2026-05-21

После Q5-фикса `key list` печатает полный 16-hex id. ux-spec выровнен под реальный вывод
(снят developer'ом в Docker): полная Unicode-рамка, ID-колонка 16 символов.

**Раунд 3a — needs-changes (2 minor):**
- В макетах `key create` (метаданные, вариант без label) и `key delete` (подтверждение + ошибки
  not found / already revoked, Long-описание, раздел «Пайп») оставался id-пример `abc123de` (8-hex)
  — противоречие с разделом «Безопасность вывода» («id — полный 16-hex») и макетом `key list`.
- Семантика ширины LABEL «14 сим.» читалась как жёсткий cap, противореча усечению «до 20 рун».

**Раунд 3b — pass (после правок cli-ux):**
- Grep `abc123de` → 0. Все id-примеры приведены к единому полному 16-hex `d7bc3a34da19d94e`
  (+ второй `e4b550b565a232b6` в `key list`). `commit abc1234` в баннере — это git short-SHA
  build-метаданных, не id ключа, нарушения нет.
- LABEL в таблице ширин переформулирована: «динамически по контенту; усечение «…» при >20 рун;
  ширина в примере 14 сим. (production-key)» — однозначно.
- Регрессий нет: макет `key list` = реальному выводу (полный 16-hex, полная рамка), 2-пробельный
  отступ метаданных create сохранён, Go-кода нет, библиотеки вывода — только из STACK.

## Verdict (раунд 3)
pass
