# Impl Notes: <task-id>

## Что реализовано
- Модуль `<имя из plan.md>` — что делает, какие контракты/сигнатуры покрыты.
- Модуль `<имя из plan.md>` — …
- (по одному пункту на модуль из `plan.md`; ничего сверх `spec.md`)

## Отклонения/эскалации
- None (если шли строго по плану)
- Отклонение/блокер: что в `plan.md` оказалось нереализуемым → почему → как эскалировано
  (кому, какое решение ждём). НИКАКИХ молчаливых обходов.

## Тесты
- Что покрыто (acceptance criteria → тесты), unit/integration.
- Команда запуска: `go test ./...` (и сборка: `go build ./...`).
- Подтверждение: все зелёные, без `skip`/`t.Skip`.

## Безопасность
- Аутентификация ключей: `crypto/rand` + `sha256(key+salt)` + salt — где в коде.
- Сравнение секретов: constant-time (`crypto/subtle`/`hmac.Equal`) — где.
- Выполнение команд: `exec.Command(bin, args...)` без shell, таймаут через `context` — где.
- Аудит-лог действия (timestamp, fingerprint ключа, команда, exit, длительность, адрес) — где.
- Права файлов секретов/ключей `0600` — где.
- (по каждому применимому пункту `SECURITY-BASELINE.ru.md`; неприменимое — пометить почему)
