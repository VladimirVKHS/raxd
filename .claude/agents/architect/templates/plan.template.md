# Plan: <название>

## Chosen Approach
Ровно один выбранный подход (3-6 строк): что строим и **почему именно так** (с опорой на spec/research).
Без «вариантов на выбор» — это решение.

## Modules
- `path/to/module1.go` — ответственность модуля (что инкапсулирует).
- `path/to/module2.go` — ответственность.
- (конкретные пути файлов/пакетов, не абстракции)

## Contracts
- `FuncName(arg1 Type1, arg2 Type2) (RetType, error)`
  - параметры: что значит каждый аргумент;
  - возврат: что возвращается при успехе;
  - ошибки: какие ошибки и как обрабатываются (без тела функции).
- `OtherFunc(...) ...` — …

## Trade-offs
- Выбрали **X** вместо **Y**, потому что …; цена выбора: …
- Новая зависимость `<lib>` (если есть): обоснование + сверка со STACK.ru.md.
